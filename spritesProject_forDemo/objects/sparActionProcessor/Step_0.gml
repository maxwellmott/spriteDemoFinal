/// @description Insert description here
// You can write your code in this editor

if (currentSpell < 0) {
	switch (currentAction) {
		case sparActions.attack:
			// check for dodge 
		break;
		
		case sparActions.dodge:
			// load dodge turn message
		break;
		
		case sparActions.rest:
			// load rest turn message
			
			// use a with statement to call the rest function from the sprite resting
		break;
	}
}
else /*		IF CURRENT ACTION IS A SPELL		*/ {	
	// check for dodge
	
	// calculate damage
	
	// apply damage and change sparMessage
	
	// check for effect function
		// if spell has effect function, call that function
}