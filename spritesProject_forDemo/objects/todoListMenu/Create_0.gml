// decode player's todoList
todoList = ds_list_create();
decode_list(player.todoList, list);

// decode player's failedSpells list

// decode player's completedSpells list

// draw the player's todoList surface